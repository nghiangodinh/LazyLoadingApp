# LazyLoadingApp
an ionic app using lazyloading way, get app information, using native bagd and network detection

* using lazy loading modules
* using in-app-browser native
* using app-version native
* using network native
* using badge native
* using toaster controller 
* using async/await for asynchronous call

